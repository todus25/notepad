owl carousal
$shop=array
         (
           array('name'=>'book','product'=>'pen'),
           array('name

var dump dekte hove
array search vvi
if(array_search('10',$y)  !== false)
 {echo 'EXISt";
  } else{ echo 'not exist';
}